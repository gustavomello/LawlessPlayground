name: LawlessPlayground
main: org.example6.example6.example6
softdepend: [Multiverse-Core, PermissionsEx]
depend: [Vault]
version: 10.3.6
commands:
   zombies:
      description: Teleport to zombies.
      usage: /zombies
      permission: example6.zombies
      permission-message: You can't do that here.
      aliases: [zombie, zomb, zom, z]
   creative:
      description: Teleport to creative.
      usage: /creative
      permission: example6.creative
      permission-message: You can't do that here.
   trycreative:
      description: Introduce self to creative ladder.
      usage: /trycreative
      permission: example6.trycreative
      permission-message: You can't do that here.
      aliases: [iwontgrief, letmebuild]
   survival:
      description: Teleport to survival
      usage: /survival
      permission: example6.survival
      permission-message: You can't do that here.
      aliases: [vanilla]
   zregen:
      description: Access zregen
      usage: /zregen
      permission: example6.zregen
      permission-message: You can't do that here.
      aliases: [zr]
   spawn:
      description: Teleport to spawn
      usage: /spawn
      permission: example6.spawn
      permission-message: You can't do that here.
   random:
      description: Teleport to a random location.
      usage: /random [maxwidth]
      permission: example6.random
      permission-message: You can't do that here.
   home:
      description: Teleport to home.
      usage: /home
      permission: example6.home
      permission-message: You can't do that here.
   mod:
      description: Send all mods a message.
      usage: /mod message
      permission: example6.modchat
      permission-message: You can't do that here.
      aliases: [modchat, mc]
   admin:
      description: Send all admins a message.
      usage: /admin message
      permission: example6.adminchat
      aliases: [adminchat, a]
      permission-message: You can't do that here.
   help:
      description: Send all admins a message.
      usage: /help [options]
      permission: example6.help
      permission-message: You can't do that here.
   rename:
      description: Renames the item held.
      usage: /rename [name]
      permission: example6.rename
   test:
      description: test
      usage: /test
      permission: example6.test
   box:
      description: test
      usage: /box
      permission: example6.box
   crash:
      description: crash
      usage: /crash
      permission: example6.crash
   sparks:
      description: sparks
      usage: /sparks
      permission: example6.sparks
   vote:
      description: vote
      usage: /vote
      permission: example6.vote
   chunk:
      description: Chunk-related tools.
      usage: /chunk [option]
      permission: example6.chunk
      permission-message: You can't do that here.
      aliases: [c]
   lore:
      description: Renames the item held.
      usage: /lore (add/remove) [message]
      permission: example6.lore
      permission-message: You can't do that here.